##  Start Ollama Nvidia CUDA needed for private gpt
- First, start Docker Desktop then run:
cd c:/privategpt/private-gpt
docker-compose --profile ollama-cuda up

## Start Aider

- If aider is not found, start is like this:

C:\Users\Alex\.local\bin\aider --sonnet